# TODO

- [ ] Create section components in `src/sections/`: Hero, Services, Doctors, Testimonials, Contact, Footer
- [ ] Update `src/app/page.tsx` to render the sections in order
- [ ] Run `npm run lint` and `npm run build` to verify everything compiles

